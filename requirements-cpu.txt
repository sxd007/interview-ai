# CPU-only dependencies for any platform
# Install: pip install -r requirements-base.txt && pip install -r requirements-cpu.txt

--index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchaudio==2.5.1+cpu
