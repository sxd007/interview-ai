# Interview AI - Dependencies
#
# IMPORTANT: This project requires platform-specific PyTorch installation.
# Please use the automatic installer script:
#
#     python scripts/install_deps.py
#
# Or manually install based on your platform:
#
# 1. macOS (Apple Silicon):
#    pip install -r requirements-base.txt
#    pip install -r requirements-mps.txt
#
# 2. Linux with NVIDIA GPU (RTX 4090):
#    pip install -r requirements-base.txt
#    pip install -r requirements-cuda.txt
#
# 3. Linux without GPU / Windows:
#    pip install -r requirements-base.txt
#    pip install -r requirements-cpu.txt
#
# For detailed instructions, see: docs/spec.md

# The following is a reference list of all dependencies.
# DO NOT install directly from this file - use the platform-specific files above.

# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.25
alembic>=1.13.0

# Auth
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ML/AI (PyTorch must be installed separately - see above)
numpy>=1.26.0
transformers>=4.36.0
accelerate>=0.25.0

# Speech/Audio
faster-whisper>=1.0.0
pyannote-audio==3.3.2
funasr>=1.3.0
demucs>=4.0.0
librosa>=0.10.1
soundfile>=0.12.1
scipy>=1.11.0

# Video
opencv-python>=4.8.0
av>=12.0.0
scenedetect>=0.6.2

# Image
pillow>=10.1.0

# PDF Generation
reportlab>=4.0.0

# Utils
tqdm>=4.66.1
httpx>=0.25.2
aiofiles>=23.2.1
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Dev (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
ruff>=0.1.0
mypy>=1.7.0
